# 📄 PDF поддержка в OpenClaw

## TL;DR
**PDF файлы уже поддерживаются из коробки!** OpenClaw 2026.2.3+ автоматически извлекает текст из PDF документов отправленных через Telegram.

## Как это работает

### Автоматическая обработка
1. Пользователь отправляет PDF в Telegram
2. OpenClaw скачивает файл через Telegram API
3. MIME-тип определяется через `file-type` (magic bytes)
4. Для `application/pdf` текст извлекается через `pdfjs-dist`
5. Извлечённый текст добавляется в контекст сообщения

### Технические детали
- **Библиотека**: pdfjs-dist@5.4.624 (уже установлена)
- **MIME определение**: file-type@21.3.0
- **Поддерживаемые форматы**: `.pdf`

## Лимиты по умолчанию

| Параметр | Значение | Описание |
|----------|----------|----------|
| maxBytes | 5 MB | Максимальный размер файла |
| maxChars | 200,000 | Максимум символов в извлечённом тексте |
| maxPages | 4 | Максимум страниц PDF для обработки |
| minTextChars | 200 | Минимум текста для успешной обработки |

## Настройка (опционально)

Если нужно изменить лимиты, добавь в `~/.openclaw/openclaw.json`:

```json
{
  "gateway": {
    "http": {
      "endpoints": {
        "responses": {
          "files": {
            "maxBytes": 10485760,
            "maxChars": 500000,
            "allowedMimes": [
              "text/plain",
              "text/markdown",
              "text/html",
              "text/csv",
              "application/json",
              "application/pdf"
            ],
            "pdf": {
              "maxPages": 10,
              "minTextChars": 100
            }
          }
        }
      }
    }
  }
}
```

## Поддерживаемые типы файлов

OpenClaw автоматически обрабатывает:
- `text/plain` (.txt)
- `text/markdown` (.md)
- `text/html` (.html)
- `text/csv` (.csv)
- `application/json` (.json)
- `application/pdf` (.pdf)

## Troubleshooting

### PDF не обрабатывается?

1. **Проверь размер файла** — должен быть < 5 MB (по умолчанию)
2. **Проверь pdfjs-dist**:
   ```bash
   ls /opt/homebrew/lib/node_modules/openclaw/node_modules/pdfjs-dist
   ```
3. **Включи verbose логи** для диагностики:
   ```bash
   OPENCLAW_VERBOSE=1 openclaw gateway restart
   ```

### Сканированные PDF (изображения)

Если PDF содержит сканированные страницы (изображения вместо текста):
- Минимум 200 символов текста требуется для успешной обработки
- Для рендеринга страниц в изображения нужен `@napi-rs/canvas` (не установлен)
- Альтернатива: используй внешний OCR (tesseract)

## Альтернативный скрипт (pdftotext)

Для использования системного `pdftotext` (poppler) вместо pdfjs-dist:

```bash
# Установка
brew install poppler

# Скрипт: tools/pdf-to-text.sh
./tools/pdf-to-text.sh /path/to/file.pdf
```

Этот скрипт полезен для:
- Больших PDF (>5MB)
- Обработки вне OpenClaw контекста
- Тестирования

## Статус

✅ **pdfjs-dist** — установлен (5.4.624)
✅ **file-type** — установлен (21.3.0)  
⚠️ **@napi-rs/canvas** — не установлен (нужен только для рендеринга страниц)

---
*Документация создана: 2026-02-07*
