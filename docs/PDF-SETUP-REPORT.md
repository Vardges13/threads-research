# 📋 Отчёт: PDF поддержка в OpenClaw

**Дата:** 2026-02-07  
**Версия OpenClaw:** 2026.2.3-1

## 🎉 Главный вывод

**PDF файлы уже поддерживаются из коробки!** Никаких дополнительных настроек не требуется.

## Что было обнаружено

### Встроенная поддержка PDF
OpenClaw автоматически обрабатывает PDF документы:

1. **pdfjs-dist@5.4.624** — уже установлен
2. **file-type@21.3.0** — для определения MIME
3. Код извлечения текста находится в `plugin-sdk/index.js`:
   - Функция `extractPdfContent()` — извлекает текст через pdfjs-dist
   - Функция `extractFileBlocks()` — обрабатывает все текстовые файлы
   - Автоматически вызывается для входящих сообщений

### Поддерживаемые форматы
- `application/pdf` (.pdf)
- `text/plain` (.txt)
- `text/markdown` (.md)
- `text/html` (.html)
- `text/csv` (.csv)
- `application/json` (.json)

### Лимиты по умолчанию
| Параметр | Значение |
|----------|----------|
| maxBytes | 5 MB |
| maxChars | 200,000 |
| maxPages | 4 |
| minTextChars | 200 |

## Что создано

### 1. Документация
- `docs/PDF-SUPPORT.md` — полная документация по PDF
- `docs/PDF-SETUP-REPORT.md` — этот отчёт

### 2. Альтернативный скрипт
- `tools/pdf-to-text.sh` — использует pdftotext (poppler)
- Полезен для файлов >5MB или тестирования

### 3. Обновлён TOOLS.md
- Добавлена секция 📄 PDF/Документы
- Обновлена структура workspace

## Как это работает

```
Telegram → OpenClaw Gateway
    ↓
fetchRemoteMedia() — скачивает файл
    ↓
saveMediaBuffer() → detectMime() — определяет application/pdf
    ↓
normalizeAttachments() — создаёт attachment object
    ↓
extractFileBlocks() → extractPdfContent()
    ↓
pdfjs-dist извлекает текст
    ↓
Текст добавляется в ctx.Body
    ↓
Агент получает текст в контексте сообщения
```

## Тестирование

Отправь PDF в Telegram — текст должен автоматически появиться в контексте.

Для диагностики:
```bash
# Verbose логи
OPENCLAW_VERBOSE=1 openclaw gateway restart

# Проверка зависимостей
ls /opt/homebrew/lib/node_modules/openclaw/node_modules/pdfjs-dist
```

## Ограничения

1. **Сканированные PDF** — если PDF содержит только изображения (сканы), текст не извлекается
   - Нужен `@napi-rs/canvas` для рендеринга страниц (не установлен)
   - Альтернатива: внешний OCR

2. **Большие PDF** — ограничение 5MB и 4 страницы
   - Можно увеличить в конфиге

## Вывод

✅ **Патч не требуется** — всё работает из коробки  
✅ **Зависимости установлены** — pdfjs-dist, file-type  
✅ **Документация создана** — для будущей справки  
✅ **Скрипт-альтернатива** — на случай особых потребностей

---
*Исследование завершено*
