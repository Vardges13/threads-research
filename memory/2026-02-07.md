# 2026-02-07 — Суббота

## PDF в OpenClaw ✅ РЕШЕНО

### Проблема
PDF файлы не обрабатывались из Telegram — ошибка `MediaFetchError: SsrFBlockedError: Blocked hostname: localhost`

### Причина
Старый **Local Bot API** (localhost:8081) был ещё запущен через LaunchAgent. SSRF-защита OpenClaw блокировала localhost.

### Решение
1. Выгрузили LaunchAgent: `launchctl unload ~/Library/LaunchAgents/com.bond.telegram-bot-api.plist`
2. Удалили старые данные: `~/.openclaw/telegram-bot-api-data/`
3. npm update уже убрал патчи из `/opt/homebrew/lib/node_modules/openclaw/dist/`
4. Перезапустили gateway

### Вывод
**PDF работает нативно** в OpenClaw 2026.2.3+ через pdfjs-dist@5.4.624:
- Лимиты: 5MB, 4 страницы, 200k символов
- Конфигурируется в `tools.media.documents`
- Никаких скриптов не нужно!

---

## Гайд по навыкам Claude

Скачали и изучили PDF "Полное руководство по созданию навыков для Claude" (33 стр., перевод t.me/aivkube)

### Сохранённые файлы
- `docs/Skill-for-Claude_RU_t.me_aivkube_30.01.2026.pdf` — оригинал
- `docs/skill-guide-text.txt` — извлечённый текст (1554 строки)
- `docs/skill-guide-summary.md` — подробное резюме

### Ключевые идеи из гайда
1. **Навык = SKILL.md** — системный промпт для Claude с инструкциями
2. **Структура**: Описание → Инструкции → Примеры → Ограничения
3. **Подключение**: skills/ папка, clawhub install, или inline через #file
4. **Триггеры**: описание в SKILL.md должно точно указывать когда использовать
5. **Тестирование**: проверять edge cases, итерировать

---

---

## Model Router Skill ✅ НАСТРОЕН (20:45)

### Что сделано
1. **Skill model-router v1.1** уже установлен в `skills/model-router/`
2. **Обновлён классификатор** под наши модели:
   - 🟢 **llama** (бесплатно) — простые задачи: "сколько", "что такое", "переведи"
   - 🟡 **sonnet** (средний) — анализ: "анализируй", "план", "исследуй" 
   - 🔴 **opus** (максимум) — код, архитектура: "создай", "построй", "сложный"
3. **Русификация** команд и интерфейса
4. **Добавлено в TOOLS.md**

### Тестирование
```bash
cd skills/model-router
python3 scripts/classify_task.py "Сколько 2+2?" 
# → 🟢 Llama (бесплатно)

python3 scripts/classify_task.py "Создай архитектуру JWT системы"
# → 🔴 Opus (максимум)
```

### Экономия
**70-80%** на API вместо постоянного использования Opus

---

## Ожидаем от Вардгеса
- [ ] Протестировать PDF после исправления
- [ ] Notion API ключ для добавления гайда в базу
